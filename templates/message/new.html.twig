{% extends 'layout.html.twig' %}

{% block title %}New Message{% endblock %}

{% block content %}
    <h1>Create new Message</h1>

    {{ include('message/_form.html.twig') }}

    <a href="{{ path('message_index') }}" class="btn btn-default">back to list</a>
{% endblock %}